# ===========================================================================
# 环境变量示例
# ---------------------------------------------------------------------------
# 用法：把本文件复制一份改名为 .env（同目录），再把下面的占位值换成你自己的。
#   Windows 命令行：copy .env.example .env
#   Mac / Linux   ：cp .env.example .env
#
# 注意：
#   1. .env 已被 .gitignore 忽略，不会提交到 GitHub —— 但 Vite 会把所有
#      VITE_ 开头的变量**打进前端 bundle**，浏览器里能看到。所以这里只能放
#      "可以公开的东西"（如一个表单网址），绝不能放密码、密钥之类。
#   2. 改完 .env 必须**重新构建**（npm run build）才会生效，热更新不够。
# ===========================================================================


# ---------------------------------------------------------------------------
# 【当前启用】意见箱表单地址 —— 社区意见箱页面
# ---------------------------------------------------------------------------
# 这是「社区意见箱」页面要内嵌的第三方表单网址（不限厂商）。
#
# 怎么拿到这个网址（二选一，都免费、都不用备案、不用建数据库）：
#   · 金数据   https://jinshuju.net   → 新建表单 → 发布 → 复制「表单链接 / 嵌入地址」
#   · 腾讯问卷 https://wj.qq.com      → 新建问卷 → 发布 → 复制「问卷链接 / 嵌入代码里的 src」
# 把复制到的链接原样粘到等号后面即可（首尾多余空格会自动忽略）。
#
# 示例：VITE_FEEDBACK_FORM_URL=https://jinshuju.net/f/AbCdEf
#
# 留空也完全没问题：页面会显示「意见箱即将开放」的友好提示，不报错、不白屏。
# 说明：意见提交全程发生在第三方表单里，本站不接收、不存储、不转发任何数据。
VITE_FEEDBACK_FORM_URL=


# ---------------------------------------------------------------------------
# 【已停用】Cloudflare Workers —— 留言板旧后端
# ---------------------------------------------------------------------------
# worker/ 目录与 wrangler.toml 的代码仍然保留，但**已不再被前端调用**。
# 停用原因：*.workers.dev 域名在中国大陆被墙，国内用户 ERR_CONNECTION_TIMED_OUT。
# VITE_COMMUNITY_API=https://daily-toolkit-community.<你的子域>.workers.dev


# ---------------------------------------------------------------------------
# 【历史遗留 / 未使用】Supabase
# ---------------------------------------------------------------------------
# 早期方案，代码中已无任何引用，保留仅作备查。
# VITE_SUPABASE_URL=https://your-project-id.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key-here
