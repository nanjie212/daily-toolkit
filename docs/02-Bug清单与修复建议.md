---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: a01ea8d25a820ebbd0f040e14016949c_1f8ac5d5878911f1b66e525400e6dd8f
    ReservedCode1: CYGdi6I9Dad5FbPSnltvoLvQ1wzsE5kDVVnciKMC+tXWjPa6G/5z+O3wVSs1/T6GLXjp/pRHpYk+MfwiA7bSIVwY7RRml1tVHjwcP2Zp9jDt+QggwwlsmouSYwESwUIVE03Y/jrkzs5rx7AHEbgl6sycCIUXrYCMks2GZctmmMhDQ7b7MPyUW+SAX58=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: a01ea8d25a820ebbd0f040e14016949c_1f8ac5d5878911f1b66e525400e6dd8f
    ReservedCode2: CYGdi6I9Dad5FbPSnltvoLvQ1wzsE5kDVVnciKMC+tXWjPa6G/5z+O3wVSs1/T6GLXjp/pRHpYk+MfwiA7bSIVwY7RRml1tVHjwcP2Zp9jDt+QggwwlsmouSYwESwUIVE03Y/jrkzs5rx7AHEbgl6sycCIUXrYCMks2GZctmmMhDQ7b7MPyUW+SAX58=
---



> ⚠️ **历史 AI 草稿，数据可能失真**：本文档为项目早期由 AI 生成的测试/问题草稿。经代码核查，所列绝大多数输入校验类 Bug **已在代码中修复**，并非当前真实状态。请以根目录 **`PROJECT_DOCUMENT.md`（权威版）** 与代码为准。以下原文保留供追溯。
>
> ---

# 普通日常工具箱 — Bug 清单与修复建议

> 检测日期：2026-07-25
> 在线地址：https://daily-toolkit.pages.dev/
> 测试方法：极限输入 + 普通人常用场景 + 控制台排查

---

## Bug 总览

| 严重度 | 数量 | 说明 |
|--------|------|------|
| 🔴 高危 | 11 | 影响计算结果正确性 / 数据丢失风险 / 无错误提示 |
| 🟡 中危 | 6 | 功能缺失、体验缺陷、可导致用户困惑 |
| 🟢 低危 | 3 | UI 瑕疵、标注错误、不影响核心功能 |

---

## 🔴 高危 Bug

### 1. 个税计算器 — 月薪 0 元仍扣除五险一金

- **现象**：月薪输 0 元，到手显示 **-2000 元**
- **原因**：五险一金字段有默认值 2000，未随月薪联动归零
- **影响**：用户若仅想测试"0 收入"场景会看到负数
- **修复**：月薪 <= 0 时跳过五险一金扣除，或直接提示"请填写有效月薪"

### 2. 房贷计算器 — 负利率无校验

- **现象**：利率输 -5%，算出「利息 ¥-57.4 亿」
- **原因**：利率字段缺少最小值校验
- **修复**：利率字段增加 `min="0"` 或业务层拦截负值

### 3. 房贷计算器 — 单位放大 10000 倍

- **现象**：输入 999,999,999 元，页面显示"贷款总额 999,999,999 **万元**"
- **原因**：大数格式化时错误地应用了 `÷10000` 逻辑并追加"万"单位
- **修复**：区分格式化显示与单位标注逻辑，或在金融场景中不使用"万"单位换算

### 4. BMI 计算器 — 负数体重无校验

- **现象**：体重输 -70kg，给出 BMI=-22.9，等级"偏瘦"
- **原因**：BMI 公式未对负数做保护
- **修复**：输入校验层拦截所有 <=0 的体重/身高值

### 5. BMI 计算器 — 超大值无上限

- **现象**：体重输 999,999kg，给出 BMI=326,530
- **原因**：未设置输入值上限
- **修复**：设置合理上限（如体重 500kg、身高 250cm）

### 6. 日期计算器 — 接受非法日期

- **现象**：输入 2024-02-30（2 月无 30 日），JS 静默修正为 2024-03-01，无任何提示
- **原因**：`new Date("2024-02-30")` 被浏览器自动推进到下个月
- **修复**：构造 Date 后检查 `date.getDate() !== 输入日`，不匹配时提示非法

### 7. 折扣计算器 — 简单折扣模式无输出

- **现象**：选「简单折扣」，输入原价 100、折扣 80，点击执行后页面无任何结果
- **原因**：简单折扣的计算分支可能缺少结果渲染逻辑
- **修复**：检查折扣计算器组件中简单折扣分支的 `setResult()` 调用

### 8. 分数计算器 — 除零不拦截

- **现象**：1 ÷ 0 直接输出 `Infinity`，无任何错误提示
- **原因**：未检查分母是否为 0
- **修复**：计算前检查分母，若为 0 则显示"除数不能为零"

### 9. 纪念日追踪 — 日期偏移 -1 天

- **现象**：输入 2026-12-25，页面显示"距今 152 天"（应为 153 天）
- **原因**：UTC 时区未处理——`new Date("2026-12-25")` 在 UTC+8 环境下解析为 UTC 0 点即本地 8 点，天数计算偏移
- **修复**：使用 `new Date(dateStr + "T00:00:00+08:00")` 或本地化日期构造函数

### 10. 纪念日追踪 — 过去日期映射到今年

- **现象**：输入 2016-05-31，页面显示"已过去 X 天"但日期变成了 2026-05-31
- **原因**：年份处理逻辑错误，覆盖了用户输入年份
- **修复**：保持输入日期原样，不做年份覆盖

### 11. 生肖星座查询 — 非闰年日期未拦截

- **现象**：输入 2001-02-29（非闰年，不存在），页面静默返回错误生肖/星座
- **原因**：与日期计算器一样的 JS Date 静默修正问题
- **修复**：同样采用 Date 构造后比对校验

---

## 🟡 中危 Bug

### 12. 服装尺码对照 — 无输入参数

- **现象**：页面仅展示静态国际尺码对照表，无身高/体重输入框
- **影响**：用户无法输入自己的数据获取个性化尺码推荐
- **建议**：增加身高/体重输入，基于数据给出美码/欧码/日码建议

### 13. 体脂率计算器 — 缺少年龄字段

- **现象**：仅有身高/体重/腰围/颈围输入，无年龄字段
- **影响**：体脂率公式依赖年龄参数（海军公式），缺少会导致计算偏差
- **修复**：增加年龄输入

### 14. 成语接龙 — 不匹配时无反馈

- **现象**：当前接龙链"花枝招展"，用户输入"一心一意"，提交后页面无任何反应——既不报错也不提示"请以展开头"
- **影响**：用户不知道输入被拒绝的原因
- **修复**：增加 `"请以「展」字开头的成语继续接龙"` 的提示

### 15. 投资收益计算器 — 定投默认值不归零

- **现象**：每月定投字段默认 ¥2000，用户仅填本金/年化/年限时计算包含了未重置的定投部分
- **影响**：纯一次性投入场景下结果偏高
- **修复**：默认值改为 0，或增加「是否包含定投」的开关

### 16. 单位转换 — 空值静默当 0

- **现象**：数值输入框清空后，工具按 0 计算并输出结果，用户无感知
- **修复**：增加非空校验，输入为空时禁用"执行"按钮

### 17. 密码生成器 — 最小长度硬钳 4

- **现象**：长度输入 1，生成结果仍为 4 位
- **原因**：代码中 `Math.max(4, userInput)` 硬钳最小值
- **影响**：用户无法生成 1~3 位密码（虽然这也合理，但应提示而非静默覆盖）
- **建议**：设置合理的 min/max 范围并向用户提示约束

---

## 🟢 低危 Bug

### 18. 时区转换 — 纽约夏令时标注错误

- **现象**：5 月份纽约标签显示 UTC-5，实际为夏令时 UTC-4
- **修复**：根据当前日期动态判断夏令时，或注明「标准时间/夏令时间」

### 19. 暗色模式 — 分类标签对比度不足

- **现象**：文字色 `#6B7280` 在背景 `#0A0A0F` 上对比度 3.5:1，低于 WCAG AA 标准的 4.5:1
- **位置**：左侧分类导航中的文字标签
- **修复**：暗色模式下将标签文字提亮至 `#9CA3AF` 或更高

### 20. 分类计数不一致

- **现象**：趣味娱乐标签显示 (11) 但实际卡片数为 12
- **原因**：`tools.js` 配置中分类计数硬编码，新增工具后未同步更新
- **修复**：计数从 `tools.js` 中动态计算而非硬编码

### 21. 引导弹窗 — 缺乏 Cookie 持久化

- **现象**：每次新会话/清除缓存后首次访问都弹出引导弹窗
- **修复**：localStorage 记录 `guide_shown=true`，已看过则不再弹

---

## 根因分析

核心问题可以归为三类：

### A. 输入校验层缺失（覆盖 11 个 Bug）

大部分工具仅依赖 HTML5 原生 `type="number"`/`type="date"` 做基础过滤，未在业务层对：
- 负数（BMI、房贷、个税）
- 非法日期（2/30、非闰年 2/29）
- 除零（分数计算器）
- 空值（单位转换）
- 极大/极小值（BMI）

做二次拦截。

**建议方案**：在 `src/utils/validators.js` 中建立统一的参数校验中间层，所有工具的「执行」按钮触发前先走校验管道：

```
输入 → 非空检查 → 范围检查 → 合法性检查 → 执行
         ↓ 失败      ↓ 失败       ↓ 失败
       提示用户     提示范围     提示非法
```

### B. 日期/时区处理不规范（覆盖 3 个 Bug）

- `new Date("2026-12-25")` 在不同浏览器中行为不一致（Safari 无法解析，Chrome 解析为 UTC）
- 纪念日计算未做时区偏移处理
- 纽约时区硬编码未判断夏令时

**建议方案**：统一使用 `dayjs` 或 `date-fns` 处理所有日期运算，指定时区参数。

### C. 默认值与边界状态（覆盖 4 个 Bug）

- 定投默认值、五险一金默认值在零输入场景下污染结果
- 简单折扣分支缺少渲染逻辑
- 分类计数硬编码未同步
- 密码长度硬钳无声

**建议方案**：建立组件开发 checklist，每条默认值必须考虑「用户不填此字段」的状态。
*（内容由AI生成，仅供参考）*
*（内容由AI生成，仅供参考）*
