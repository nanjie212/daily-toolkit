> ⚠️ **历史 AI 草稿，数据可能失真**：本文档为项目早期由 AI 生成的"对话整理"草稿，其中工具数量（写成了 71/10 分类）、部署平台（Cloudflare Pages）、技术栈（`functions`/`D1`/`Supabase` 后端）等**与代码不符**——当前项目已清理后端、改为纯静态 GitHub Pages 部署。请以根目录 **`PROJECT_DOCUMENT.md`（权威版）** 为准。以下原文保留供追溯。
>
> ---

# 普通日常工具箱 —— 完整开发对话记录整理

> 本文档整理了项目从"工具搜索功能审查"到"项目打包交付"期间的全部对话内容，
> 按时间顺序记录每一次需求、排查、修复与部署。

---

## 一、项目概述

**项目名称**：普通日常工具箱（daily-toolkit）

**项目定位**：一个集成 71 个实用工具的纯前端 PWA 工具箱网站，覆盖生活、趣味、魔法、AI、图像、PDF、媒体、文档、身份、编码等 10 大分类。

**部署地址**：Cloudflare Pages（GitHub main 分支自动部署）

---

## 二、技术栈

| 层级 | 技术选型 |
|------|---------|
| 前端框架 | React 18 + TypeScript |
| 样式 | Tailwind CSS 3 |
| 构建 | Vite 6 |
| 状态管理 | Zustand |
| 路由 | React Router DOM（Hash 模式） |
| PWA | vite-plugin-pwa（workbox generateSW，skipWaiting/clientsClaim） |
| 后端 | Cloudflare Pages Functions（/functions/）+ D1 SQLite |
| 字符处理 | Intl.Segmenter（ES2022，按字形簇拆分 emoji）、pinyin-pro（拼音搜索） |
| API 端点 | /api/messages（CRUD+点赞+回复+鼓励）、/api/stats（访问统计） |
| 音频 | Web Audio API（AudioContext/OscillatorNode，成语接龙音效） |

---

## 三、对话历史（按时间顺序）

### 第 1 轮：审查工具搜索功能

**用户需求**：审查搜索功能的完整性。

**排查与修复**：
- 发现搜索"二"搜不到二维码工具，原因是拼音搜索未覆盖 `tool.id` 字段。
- 修复：`matchPinyin(...)` 加入 `tool.id`，引入 `matchPinyin` 替代 `includes`。
- 新增 `Ctrl+K`/`Cmd+K` 快捷键聚焦搜索框。
- Home.tsx 新增 `useLocation` 读取 URL 参数 `?category=` 实现分类跳转。

### 第 2 轮：特殊符号空白格子修复

**用户反馈**：特殊符号大全很多空白格子，点击后有连锁反应；"打'二'搜不到二维码"。

**两轮修复**：
1. **第 1 层**：`Array.from(symbols)` 按码点拆分变体选择器 emoji → 改用 `Intl.Segmenter` 按字形簇拆分。
2. **第 2 层**：`® 特殊`/`⌨ 键盘` 数据源包含大量无字体支持的 Unicode 字符 → 替换数据源 + 新增 `isPrintableGrapheme()` 过滤函数，过滤控制字符/变体选择器/PUA/零宽字符。

**用户追问**："还是有很多空白的。是我没刷新好吗还是别的问题" → 确认是数据源问题，第 2 轮修复后提醒 Unregister Service Worker + 硬刷新。

### 第 3 轮：深度检查所有工具

**用户需求**："现在开始检查其他工具或者网页整个的功能上还有没有什么问题。找到之后直接进行修复"。

**修复**：
- BMI 负数/超大值校验
- 单位转换空值校验
- 日期计算器 `isValidDate` 双重校验
- 房贷计算器万元→元单位修正 + 负利率校验
- 折扣计算器空值守卫

### 第 4 轮：功能可扩展性咨询

**用户需求**："这个工具还有没有别的可拓展的功能？"

**建议方向**（高优先级）：
1. 文件哈希校验（MD5/SHA256）
2. JSON 格式化/校验器
3. 正则表达式测试器
4. 颜色工具（HEX/RGB/HSL 互转 + 调色板）

### 第 5 轮：外部测试报告修复（第一批）

**用户需求**：提供外部测试报告，涉及搜索框交互异常、引导弹窗、分类计数偏差。

**修复**：
- 搜索框容器加 `z-[60]` 确保在引导弹窗之上；`onFocus` 自动关闭弹窗。
- 分类标签暗色模式对比度提升：`text-gray-400` → `text-gray-300`。
- 精确扫描确认趣味娱乐确为 11 个工具（排除报告计数偏差）。

### 第 6 轮：输入校验类 Bug 修复（第二批）

**用户需求**：提供深度排查报告，含 BMI 负数、房贷负利率、日期非法值等 10 项问题。

**修复**：统一拦截负数/空值/超大值/非法日期。

### 第 7 轮：更深度自我测试

**用户需求**："我用别的工具来测试的，有什么问题没有？你需要再想办法把这个网页的问题重新测试一下，一定要更深入的测试啊"。

**发现并修复 5 个隐藏 Bug**：

| Bug | 描述 | 修复文件 |
|-----|------|---------|
| 7 | Developer 沙盒 `console.log` 异常时未恢复 | Developer.tsx |
| 8 | textToSpeech 语速未限制 (0.1-10) | aiTools.ts |
| 9 | countdown 接受非法日期 (2024-02-30) | lifeTools.ts |
| 10 | pomodoroTimer 负数无校验 | lifeTools.ts |
| 11 | IdiomChainGame AudioContext 内存泄漏 | IdiomChainGame.tsx |

### 第 8 轮：又一轮深度审查（6 个 Bug）

**用户需求**：继续深度审查。

**修复**：
| Bug | 描述 |
|-----|------|
| 1 | Home.tsx 自动清除逻辑覆盖 URL 参数 `?category=` |
| 2 | pixelArtConvert pixelSize=0 导致除零/height=0 |
| 3 | photoRestore/photoToSketch/passwordGenerator 缺少参数边界校验 |
| 4 | ledMarquee bgOpacity 无范围校验 |
| 5 | periodTrackerCalc cycleDays/periodDays 无校验 |
| 6 | funTools.ts 死代码（kinshipMap + chineseIdioms） |

### 第 9 轮：外部测试报告 v1.2.1（10 个真实 Bug）

**用户需求**：提供 v1.2.1 测试报告，含个税负到手、分数除零、纪念日 UTC、生肖非法日期等。

**修复**：
| Bug | 描述 |
|-----|------|
| 1 | 个税计算器 socialInsurance > salary 导致负到手 |
| 2 | 分数计算器 1÷0 输出 Infinity |
| 9 | 纪念日追踪 UTC 时区偏移 -1 天 |
| 10 | 生肖星座查询非闰年 2001-02-29 未被拒绝 |
| 11 | 投资收益计算器每月定投默认 2000 污染纯一次性投入 |
| 15 | 成语接龙不验证用户输入是否为有效成语 |
| 16 | 密码生成器硬钳无提示 |
| 17 | 时区转换标签写死 UTC-5 未体现夏令时 |

**核实已在前期修复**（报告命中 PWA 缓存）：BMI 负数/超大值、日期计算器非法日期、房贷负利率/单位、单位转换静默、暗色对比度、趣味计数、引导弹窗持久化。

### 第 10 轮：导航问题修复

**用户需求**："直接访问社区留言这个模块的话，不能回到主页。还有其他页面好像也不能直接回到主页。"

**修复**：
- FooterBar 新增"首页"导航链接（带高亮状态）。
- Community.tsx / Developer.tsx 的返回按钮从 `window.history.back()` 改为 `navigate('/')`。
- Market.tsx 添加返回首页按钮。

### 第 11 轮：继续修复（5 个 Bug）

**用户需求**："继续修复网页中的问题"。

**修复**：
| Bug | 描述 | 修复文件 |
|-----|------|---------|
| 1 | OutputPanel clipboard 操作无 try/catch | OutputPanel.tsx |
| 2 | videoToGif 超长 duration 产生重复帧 | mediaTools.ts |
| 3 | heartRateZoneCalc restHR=0 无校验 | healthTools.ts |
| 4 | dueDateCalc 未来日期显示负孕周 | healthTools.ts |
| 5 | ToolDetail 两个复制按钮共用 copied 状态 | ToolDetail.tsx |

### 第 12 轮：项目状态盘点

**用户需求**："这个项目现在怎么样了"。

**结论**：项目稳定可用，71 个工具、10 大分类，经历 10 轮提交修复约 30+ 个 Bug，构建验证通过，已部署。

### 第 13 轮：项目打包交付

**用户需求**："把这个项目的所有资料都放到我桌面的普通日常工具箱（新建）这个文件夹里"。

**说明**：当前为远程沙箱环境，无法直接访问本地桌面，改为打包完整源码 + 对话整理文档供下载。

---

## 四、修复 Bug 完整清单

### 输入校验类
- BMI 负数体重 / 超大值（>500kg）
- 房贷负利率 / 单位错误（万元→元）
- 折扣计算器空值守卫
- 单位转换非数字静默处理
- 日期计算器非法日期（2024-02-30）
- 个税 socialInsurance > salary
- 分数计算器除零 Infinity
- 番茄钟负数无校验
- 心率区间 restHR=0
- 预产期未来日期
- 倒计时非法日期
- 经期推算荒谬值
- 密码长度无上限
- 照片修复/素描强度无界
- 像素画除零/零高度
- LED 弹幕透明度越界
- TTS 语速越界

### 交互/导航类
- 搜索框被引导弹窗遮挡
- 搜索"二"找不到二维码（拼音未覆盖 id）
- 社区/市场/开发者页面无法返回首页
- 复制按钮状态串扰
- clipboard 异常未捕获

### 数据/UI 类
- 特殊符号空白格子（字形簇拆分 + 数据源清理）
- 暗色模式分类标签对比度不足
- 时区标签未体现夏令时

### 健壮性/内存类
- Developer 沙盒 console.log 永久丢失
- AudioContext 内存泄漏
- videoToGif 重复帧
- 成语接龙无输入校验

### 死代码清理
- funTools.ts 模块级 kinshipMap / chineseIdioms 未使用

---

## 五、项目最终状态

| 维度 | 状态 |
|------|------|
| 工具总数 | 71 个 |
| 工具分类 | 10 个 |
| 构建验证 | ✅ TypeScript + Vite 通过 |
| 输入校验 | ✅ 系统性加固 |
| 路由导航 | ✅ 所有页面可返回首页 |
| 内存安全 | ✅ AudioContext 单例、URL 对象释放 |
| 异常处理 | ✅ clipboard/API 调用有 catch |
| 死代码 | ✅ 已清理 |
| 部署 | ✅ GitHub main → Cloudflare Pages 自动部署 |
| 版本 | v1.2.1 |

---

## 六、部署信息

### 基础信息

| 项 | 值 |
|----|-----|
| **项目名称** | 普通日常工具箱（daily-toolkit） |
| **当前版本** | v1.2.1 |
| **部署平台** | Cloudflare Pages |
| **部署方式** | GitHub main 分支自动部署（push 即触发） |
| **GitHub 仓库** | `https://github.com/nanjie212/daily-toolkit.git` |
| **路由模式** | Hash 模式（React Router HashRouter） |

### 后端 / 数据

| 项 | 值 |
|----|-----|
| **后端方案** | Cloudflare Pages Functions（`/functions/api/`） |
| **数据库** | Cloudflare D1（SQLite） |
| **数据库 schema** | `d1-schema.sql`（留言板 + 访问统计） |
| **Supabase 迁移** | `supabase-migration.sql`（备用方案） |
| **API 端点** | `/api/messages`（CRUD + 点赞 + 回复 + 鼓励）<br>`/api/stats`（访问统计） |

### PWA / 缓存

| 项 | 值 |
|----|-----|
| **PWA 插件** | `vite-plugin-pwa` |
| **模式** | workbox `generateSW` |
| **Service Worker** | `skipWaiting: true` / `clientsClaim: true` |
| **预缓存条目** | 42 个，约 3.4 MB |
| **更新策略** | 部署后需 Unregister Service Worker + 硬刷新（Ctrl+Shift+R） |

### 构建产物

| 项 | 值 |
|----|-----|
| **构建命令** | `npm run build`（Vite 6） |
| **构建耗时** | ~7-8 秒 |
| **产物目录** | `dist/` |
| **主要 chunk** | index（~480KB）、pdfTools（~465KB）、opencc（~1.1MB）、pdf（~365KB） |
| **总产物大小** | ~3.4 MB（gzip 后约 1 MB） |

### 静态资源

| 项 | 路径 |
|----|------|
| **支付宝赞赏码** | `public/alipay-donate.jpg`、`public/alipay-donate.svg`（根目录也有一份） |
| **微信赞赏码** | `public/wechat-donate.jpg`、`public/wechat-donate.svg`（根目录也有一份） |
| **网站图标** | `public/favicon.svg` |
| **PWA 清单** | `public/manifest.json` |

### 部署后刷新策略

由于 PWA Service Worker 缓存，每次部署后用户可能看到旧版本。需要：
1. 打开浏览器 DevTools（F12）
2. 进入 Application → Service Workers
3. 点击 Unregister
4. 硬刷新页面（Ctrl+Shift+R）

---

## 七、项目愿景与核心理念

### 7.1 产品定位

**"普通日常工具箱"** 这个名字体现了用户的核心想法：
- **"普通"**：不追求花哨，专注实用性
- **"日常"**：解决日常生活中的实际问题
- **"工具箱"**：集成化、一站式解决方案

用户希望这是一个**真正好用**的工具集合，而不是功能堆砌。

### 7.2 质量标准

从对话中可以看出用户对质量的高要求：
- **反复强调"更深入测试"**：不满足于表面修复，要求系统性排查
- **主动使用外部工具测试**：说明重视独立验证，不依赖单一测试方法
- **关注用户体验细节**：如"社区留言不能回到主页"这种导航问题

### 7.3 知识管理意识

用户要求整理对话记录，说明：
- 重视**知识沉淀**和**过程记录**
- 希望未来能回顾和参考
- 认为开发过程本身也有价值

---

## 八、技术决策背后的思考

### 8.1 为什么选择这些技术栈？

| 技术选择 | 背后原因 |
|---------|---------|
| **Hash 路由模式** | 部署灵活性高，不需要服务器配置 URL 重写，适合静态托管 |
| **Zustand 状态管理** | 比 Redux 简单，API 直观，适合中小型项目 |
| **Cloudflare Pages** | 免费额度充足，自动部署，全球 CDN，与 D1 数据库集成好 |
| **D1 SQLite** | 与 Cloudflare 生态深度集成，无需额外配置，适合轻量级数据 |
| **Tailwind CSS** | 原子化 CSS，开发效率高，样式一致性好 |
| **Vite** | 构建速度快，HMR 体验好，配置简单 |

### 8.2 关键技术决策

#### 为什么用 `Intl.Segmenter` 而不是 `Array.from`？

**问题**：特殊符号工具中，emoji 和变体选择器字符被错误拆分，导致空白格子。

**解决思路**：
- `Array.from()` 按 Unicode 码点拆分，会把组合 emoji 拆开
- `Intl.Segmenter` 按**字形簇**（grapheme cluster）拆分，正确处理组合字符
- 这是 ES2022 的新特性，专门解决这类问题

#### 为什么用 `matchPinyin` 而不是 `includes`？

**问题**：搜索"二"找不到"二维码"工具。

**解决思路**：
- `includes` 只支持精确匹配
- `matchPinyin` 支持拼音搜索（er → 二 → 二维码）
- 还支持模糊匹配，提升搜索体验

#### 为什么 AudioContext 要单例？

**问题**：成语接龙游戏每次播放音效都创建新的 AudioContext，导致浏览器报错。

**原因**：
- 浏览器限制同时存在的 AudioContext 数量（通常 6 个）
- 超过限制后会静默失败
- 解决方案：用 `useRef` 保存单一实例，复用同一个 AudioContext

### 8.3 Bug 修复的系统性方法

#### 输入校验的统一模式

不是逐个修复，而是建立统一的校验思路：
```typescript
// 1. 检查必填字段
if (!field) return { success: false, error: '请输入xxx' };

// 2. 检查数值范围
if (value <= 0 || value > MAX) return { success: false, error: 'xxx应在合理范围' };

// 3. 检查日期有效性
if (!isValidDate(date)) return { success: false, error: '日期格式无效' };

// 4. 检查业务逻辑
if (socialInsurance > salary) return { success: false, error: '五险一金不应超过月薪' };
```

#### 特殊符号问题的两层解决

**第一层**：修复拆分逻辑
- 从 `Array.from()` 改为 `Intl.Segmenter`

**第二层**：清理数据源
- 替换包含不可见字符的分类（如 `® 特殊`、`⌨ 键盘`）
- 添加 `isPrintableGrapheme()` 过滤函数

**启示**：复杂问题往往需要多层解决，不能只修表面。

---

## 九、用户体验考量

### 9.1 导航体验

用户反馈"社区留言不能回到主页"，说明：
- **导航必须清晰**：每个页面都要有明确的返回路径
- **不能依赖浏览器历史**：`window.history.back()` 在直接访问时会失效
- **解决方案**：使用 `navigate('/')` 确保始终能返回首页

### 9.2 缓存问题

PWA 缓存导致部署不生效，用户多次反馈"还是有很多空白的"：
- **问题**：Service Worker 缓存旧版本，用户看不到更新
- **解决**：每次部署后需要 Unregister + 硬刷新
- **思考**：是否应该实现自动更新提示？

### 9.3 错误反馈

成语接龙工具不验证输入，用户不知道哪里错了：
- **问题**：静默失败，用户体验差
- **解决**：添加明确的错误提示
- **原则**：任何用户操作都应该有明确的反馈

---

## 十、对话中的思想火花

### 10.1 "一定要更深入的测试"

这句话体现了：
- **不满足于表面**：要找到根本原因
- **系统性思维**：不是修一个 Bug，而是检查一类问题
- **质量意识**：宁可多花时间，也要确保稳定

### 10.2 外部测试工具的价值

用户主动用其他工具测试，说明：
- **独立验证**很重要：开发者自己测试容易有盲点
- **多角度测试**：不同工具可能发现不同问题
- **持续改进**：测试不是一次性的，而是持续的过程

### 10.3 文档的价值

用户要求整理对话记录，说明：
- **过程比结果更重要**：开发思路、决策过程都有价值
- **知识需要沉淀**：否则容易遗忘
- **可追溯性**：未来遇到问题可以回顾

---

## 十一、后续可扩展方向（未实施）

### 11.1 高优先级

1. **文件哈希校验**（MD5/SHA256）
   - 用途：验证文件完整性
   - 技术：Web Crypto API

2. **JSON 格式化/校验器**
   - 用途：开发者工具
   - 功能：格式化、压缩、语法检查

3. **正则表达式测试器**
   - 用途：开发和调试正则
   - 功能：实时匹配、高亮、解释

4. **颜色工具**
   - 用途：设计和开发
   - 功能：HEX/RGB/HSL 互转、调色板生成

### 11.2 中优先级

5. **Markdown 编辑器**
   - 实时预览
   - 导出 HTML/PDF

6. **代码片段管理**
   - 收藏常用代码
   - 分类管理

7. **单位换算增强**
   - 更多单位类型（温度、压力、能量等）
   - 自定义单位

### 11.3 低优先级

8. **离线功能增强**
   - 完全离线可用
   - 本地数据同步

9. **多语言支持**
   - 国际化（i18n）
   - 多语言工具名称

10. **用户自定义工具**
    - 允许用户创建自己的工具
    - 工具市场

---

## 十二、经验总结

### 12.1 开发经验

1. **输入校验是基础**
   - 永远不要信任用户输入
   - 建立统一的校验模式
   - 边界条件要全面考虑

2. **缓存问题要重视**
   - PWA 缓存会导致更新不生效
   - 需要明确的更新策略
   - 考虑自动更新提示

3. **错误处理要完善**
   - 所有异步操作都要 catch
   - 给用户明确的错误提示
   - 不要静默失败

4. **代码质量要持续改进**
   - 定期审查和重构
   - 删除死代码
   - 统一代码风格

### 12.2 测试经验

1. **边界条件测试**
   - 空值、负数、极大值
   - 非法日期、非法格式
   - 除零、溢出

2. **用户体验测试**
   - 导航是否清晰
   - 错误提示是否明确
   - 操作反馈是否及时

3. **多环境测试**
   - 不同浏览器
   - 不同设备
   - 不同网络条件

### 12.3 沟通经验

1. **理解用户真实需求**
   - 用户说"空白格子"，实际是字符拆分问题
   - 用户说"不能回到主页"，实际是导航设计问题
   - 要透过现象看本质

2. **主动沟通**
   - 发现问题主动报告
   - 提供多个解决方案
   - 解释技术决策的原因

3. **文档化**
   - 重要决策要记录
   - 问题修复要总结
   - 经验教训要沉淀

---

*本文档由对话记录整理生成，记录了项目从审查到交付的完整过程，包括技术决策、用户体验考量、思想火花和经验总结。*
