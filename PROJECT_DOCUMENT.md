# 普通日常工具箱 — 产品文档（权威版）

> 版本：v1.2.1 ｜ 更新日期：2026-07-31
> 在线地址：https://nanjie212.github.io/daily-toolkit/
> 仓库：`github.com/nanjie212/daily-toolkit`
>
> ⚠️ **本文档为项目权威说明。** 同目录下的 `01-项目概览与架构分析.md`、`02-Bug清单与修复建议.md`、`03-完整测试记录.md`、`04-项目原始资料汇编.md`、`对话记录整理.md`、`ROADMAP_PHASE1_2.md` 均为早期 **AI 生成的草稿**，其中工具数量、分类数、部署平台、技术栈等数据与代码不符，**请以本文为准**。

---

## 一、项目定位

**普通日常工具箱** 是一个**纯前端、纯本地运行**的 Web 端多功能工具集合站。所有工具 100% 在浏览器内完成计算与处理，**数据不出本地、无需联网、无需注册**。首次加载后可离线使用（PWA）。

- 隐私优先：无任何后端、无第三方 API、无埋点统计、无广告
- 永久免费：无付费墙、无次数限制、无水印
- 即开即用：浏览器打开即用，可"添加到桌面"作为 PWA 应用

---

## 二、核心特性

| 特性 | 说明 |
|------|------|
| **100% 本地运行** | 全部计算/处理在浏览器内完成，不上传任何数据 |
| **完全离线可用** | Service Worker 预缓存资源，断网仍可用 |
| **零后端 / 零 API** | 无服务器依赖，无第三方接口调用（社区留言仅存 localStorage） |
| **66 个内置工具** | 覆盖日常、理财、健康、图片/PDF、趣味 5 大分类 |
| **智能搜索** | 支持中文 / 全拼 / 拼音首字母；`Ctrl+K` / `Cmd+K` 快速聚焦 |
| **个人化** | 收藏、固定、最近使用（均本地保存） |
| **主题与响应式** | 暗色 / 亮色主题，手机 / 平板 / 桌面自适应 |
| **社区留言板** | 纯本地存储的留言/点赞/回复（不联网、不共享） |
| **开发者中心** | 自定义工具本地持久化 |

---

## 三、技术栈（实测）

| 层面 | 技术 | 说明 |
|------|------|------|
| 框架 | React 18 + TypeScript | 函数组件 + Hooks |
| 构建 | Vite 6 | 快速构建 |
| 样式 | Tailwind CSS 3 | 原子化 CSS，支持 `dark:` 暗色变体 |
| 状态管理 | Zustand | 轻量全局状态 |
| 路由 | React Router（**Hash 模式**） | 适配 GitHub Pages 子路径 `/daily-toolkit/`，无需服务端重写 |
| PWA | vite-plugin-pwa | Service Worker 预缓存，可安装到桌面 |
| 存储 | localStorage | 收藏/固定/最近/留言/主题偏好 |
| 部署 | **GitHub Pages**（`gh-pages` 分支） | 仓库 `nanjie212/daily-toolkit`，`base: '/daily-toolkit/'` |

> 注：早期方案曾含 Cloudflare Pages Functions / D1 / Supabase 等后端依赖，已于 v1.2.x 清理移除，当前为**纯静态、零后端**。

---

## 四、工具全景（共 66 个，5 大分类）

分类计数由代码动态统计（非硬编码），下列为实测分布：

| 分类 | id | 数量 | 典型工具 |
|------|-----|------|---------|
| 日常必备 | `everyday` | **29** | 二维码、单位换算、房贷/个税计算、BMI、日期计算、随机/抽奖、番茄钟、时区转换、密码生成、敏感信息脱敏、文字转语音、繁简转换等 |
| 图片与 PDF | `image` | **15** | 图片压缩/格式转换/裁剪、证件照、加水印/去水印、图片转 PDF、OCR、PDF 拆分/加密/签名/压缩等 |
| 趣味娱乐 | `fun` | **11** | 今天吃什么、表情包、成语接龙、亲戚称呼、词云、LED 跑马灯、幸运大转盘、手持弹幕等 |
| 理财计算 | `finance` | **6** | 社保/五险一金、存款利息、提前还贷、年终奖个税、投资回报、购车税费 |
| 健康生活 | `health` | **5** | BMI、卡路里、体脂率、基础代谢(BMR)、心率区间（含预产期、经期推算等扩展） |

> 完整实时列表以站点首页为准。每个工具独立路由（如 `#/tool/bmi`）。

---

## 五、质量状态（实测）

经代码核查，早期测试报告（02/03）中列出的绝大多数输入校验类 Bug **已在代码中修复**（负数/空值/超大值/非法日期/除零等均已加固）。最近一轮优化又修复了：

- ✅ **纪念日/预产期/经期/日期推算等日期显示**：原用 `toISOString()`（UTC）导致中国时区显示少一天，已统一改为本地时区格式化（`src/lib/date.ts`）
- ✅ **暗色模式文字对比度**：分类计数、搜索图标、提示文字等提亮（`dark:text-gray-300/400`），满足可读性
- ✅ **BMI 归类**：从 `everyday` 修正为 `health`，与分类定义一致

输入校验原则：所有工具的"执行"前均做非空、范围、合法性（含非法日期 `2024-02-30`、非闰年 `2001-02-29` 等）拦截，不再静默输出错误结果。

---

## 六、本地开发与部署

```bash
npm install        # 安装依赖
npm run dev        # 本地开发 (Vite)
npm run build      # 构建到 dist/
npm run deploy     # 构建并推送到 gh-pages 分支（需配置 Git 远程，推荐 SSH）
```

部署说明：
- 站点由 `gh-pages` 分支的 `dist/` 提供，GitHub Pages 源设为 `gh-pages` 分支
- 使用 **SSH** 推送（已为用户配置 `.ssh/config` 走 443 端口，绕过 22 端口封锁），无需明文 token
- 路由为 Hash 模式，GitHub Pages 无需 SPA 重写

---

## 七、产品铁律（不可违背）

- ❌ 不上传用户数据（所有计算在浏览器内完成）
- ❌ 不收集用户行为（零埋点、零分析脚本）
- ❌ 不加广告（运行成本为零）
- ❌ 不加付费墙 / 会员体系
- ❌ 不接入任何第三方 API（所有功能纯前端实现）

---

*本文档由项目维护者根据实际代码核查整理，数据以代码为准。*
